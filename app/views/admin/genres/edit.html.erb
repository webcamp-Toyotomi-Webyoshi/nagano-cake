<h2>ジャンル一覧・追加</h2>
<div class="genre-create">
  <%= form_for(@genre, url:admin_genre_path) do |f| %>
    <%= f.label :"ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit :変更を保存 %>
  <% end %>
</div>