  <div class="field">
    <span>氏名</span><%= @customer.full_name %>
  </div>

  <div class="field">
    <span>カナ</span><%= @customer.full_name_kana %>
  </div>

  <div class="field">
    <span>郵便番号</span><br /><%= @customer.post_cord %>
  </div>

  <div class="field">
    <span>住所</span><%= @customer.address %>
  </div>

  <div class="field">
    <span>電話番号</span><%= @customer.telephone %>
  </div>

  <div class="field">
    <span>メールアドレス</span><%= @customer.email %>
  </div>
  <% if @customer.customer_status == false %>
  <div class="field">
    <span>会員ステータス</span><span>有効</span>
  </div>
  <% else %>
  <div class="field">
    <span>会員ステータス</span><span>退会</span>
  </div>
  <% end %>
  <%= link_to "編集する", edit_admin_customer_path %>
  <%= link_to "注文履歴一覧を見る", admin_customer_orders_path %>